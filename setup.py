from setuptools import setup

setup(
    name="procwrap",
    version="0.1.0",
    description="This is a library that uses subprocess to automatically capture the complete output.",
    author="BONIHANI",
    packages=["procwrap"],
    python_requires=">=3.7.3",
)